[pytest]
# Tests for the FastMCP server layer (MCP venv: mcp_server/.venv)
# Run from project root: source mcp_server/.venv/bin/activate && pytest mcp_server/tests/
testpaths = tests
asyncio_mode = auto
