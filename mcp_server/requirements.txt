# Learning Path Generator — custom MCP server
# Python 3.10+ required (FastMCP 3.x)
# Pinned with ~= (compatible release) to the versions verified working on 2026-06-14.
# ~=X.Y allows patch updates (X.Y.Z) but blocks minor/major version changes.
fastmcp~=3.3
google-api-python-client~=2.197
google-auth~=2.53
httpx~=0.28
python-dotenv~=1.2
