# Streamlit app + LangGraph agent
# Pinned with ~= (compatible release) to the versions verified working on 2026-06-14.
# ~=X.Y allows patch updates (X.Y.Z) but blocks minor/major version changes that
# commonly introduce breaking API changes in the LangChain ecosystem.
langchain~=1.3
langgraph~=1.2
langchain-mcp-adapters~=0.2
langchain-google-genai~=4.2
streamlit~=1.58
python-dotenv~=1.2
